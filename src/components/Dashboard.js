import React, { Component } from "react";
import { connect } from "react-redux";
import { Field, reduxForm, formValueSelector } from "redux-form";
import { Checkbox, RadioGroup, Select, Toggle, TextField, Switch, FormControlLabel } from "redux-form-material-ui";


/**
 *  The dashboard that includes the directory query options.
 */
class Dashboard extends Component {
    render() {
        return (
            <div>
                <p>HEY THEREE!</p>
            </div>
        )
    }
}

export default Dashboard;